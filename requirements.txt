fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pypdf>=4.2
python-docx>=1.1

# Optional, install only if you want them:
#   pip install sentence-transformers   # dense retrieval alongside BM25
#   pip install anthropic               # written answers instead of quotes
#   pip install openai
